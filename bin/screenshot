#!/bin/bash
# A bemenu script for taking screenshots for wayland
# DEPS: xdg-utils, wl-clipboard, slurp, grim, bemenu
SCREENSHOT_DIR=~/Screenshots
MENU_CMD="tofi --prompt screenshot"

function main() {

pos=$(slurp)

action=$(echo $'retake\nclipboard\nfile\nfile and open' | $MENU_CMD ) 

if [ "$action" = "retake" ] ; then
   main
elif [ "$action" = "clipboard" ] ; then
   echo "$pos" | grim -g - - | wl-copy
elif [ "$action" = "file" ] || [ "$action" = "file and open" ] ; then
   mkdir "$SCREENSHOT_DIR"
   path="$SCREENSHOT_DIR/$(date '+%H:%M:%S_%m-%d-%Y').png"
   echo "$pos" | grim -g - "$path"
   if [ "$action" = "file and open" ] ; then
      xdg-open "$path"
   fi
fi

}

main
